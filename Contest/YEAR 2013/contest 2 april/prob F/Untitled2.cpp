#include <stdio.h>
#include <string.h>
#include <algorithm>
#define MAX 1002
using namespace std;
int main()
{
    int
    char st[MAX],st1[MAX];
    gets(st);
    l=strlen(st);
    for(i=0;i<l;i++)
    {

    }
    return 0;
}
